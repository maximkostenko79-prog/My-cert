# 🎁 Бот для выдачи именных подарочных сертификатов

Бот автоматически генерирует и выдает именной сертификат (PNG) в Telegram сразу после успешной оплаты через платежную систему **Prodamus**.

## ✨ Возможности
*   **Автоматизация:** Клиент вводит имя -> получает ссылку -> оплачивает -> получает сертификат.
*   **Prodamus:** Полная интеграция (передача чеков, отправка email с инструкцией, проверка статуса).
*   **Сертификаты:** Генерация изображения с именем и уникальным номером (0001, 0002...).
*   **Админка:** Выгрузка полной истории заказов в файл (`/listusers`).
*   **Розыгрыш:** Автоматическое приглашение в розыгрыш призов после покупки.

---

## 🚀 Инструкция по запуску на Render.com

### 1. Подготовка файлов
Убедитесь, что в корне репозитория лежат:
*   `sertif.png` — Шаблон сертификата.
*   `font.ttf` (или название вашего шрифта) — Шрифт для текста на картинке.
*   `requirements.txt` — Список зависимостей.

### 2. Создание Web Service
1. Создайте **Web Service** на Render, подключив этот репозиторий.
2. **Environment:** Python 3.
3. **Build Command:** `pip install -r requirements.txt`
4. **Start Command:** `python main.py`

### 3. Переменные окружения (Environment Variables)
В настройках сервиса на Render добавьте:
*   `BOT_TOKEN` — Токен вашего Telegram-бота (от @BotFather).
*   `PRODAMUS_SECRET_KEY` — Секретный ключ из настроек платежной страницы Prodamus.
*   `PYTHON_VERSION` — `3.10.0` (или новее).

### 4. 💾 Важно: Сохранение Базы Данных (Disk)
Чтобы база данных не удалялась при перезапуске бота:
1. В меню Render перейдите в раздел **Disks**.
2. Создайте новый диск:
    *   **Name:** `data`
    *   **Mount Path:** `/var/data`
    *   **Size:** 1 GB (хватит надолго).
3. Подключите этот диск к вашему сервису.

---

## ⚙️ Настройка в Prodamus

1. Зайдите в настройки вашей платежной страницы (раздел "Настройки").
2. В поле **"URL адреса для уведомлений"** вставьте ссылку: https://ВАШ-ПРОЕКТ.onrender.com/prodamus-webhook
3. *(Замените `ВАШ-ПРОЕКТ` на реальный адрес, который выдал Render)*.
3. Скопируйте **Секретный ключ** и добавьте его в переменные окружения бота.
4. Нажмите "Сохранить".

---

## 👮‍♂️ Администрирование

В боте доступна команда для администратора (доступ только по вашему ID):

*   `/listusers` — Бот пришлет файл `all_orders.txt`, содержащий полный список всех заказов (оплаченных и нет) с именами и номерами сертификатов.

---

## 🛠 Структура проекта
*   `main.py` — Основная логика бота, вебхуки и админка.
*   `database.py` — Работа с SQLite (сохранение в `/var/data/users.db`).
*   `certificate_generator.py` — Рисование имени и номера на `sertif.png`.
