# Бот для выдачи именных подарочных сертификатов

Автоматически выдаёт PDF-сертификаты в Telegram после оплаты через Продамус.

## 🔧 Настройка

1. **Переменные окружения (в Railway):**
   - `BOT_TOKEN` — токен Telegram-бота (уже частично указан в коде)
   - `PRODAMUS_OFFER_ID` — ID товара в Продамусе (с поддержкой произвольной суммы)
   - `WEBHOOK_SECRET` — секретный ключ webhook'а из Продамуса (опционально, для безопасности)
   - `BASE_URL` — URL вашего приложения в Railway (например: `https://my-cert-bot.up.railway.app`)

2. **Шаблон сертификата:**
   - Загрузите файл `sertif.png` в корень репозитория (сделаем далее)

3. **В Продамусе:**
   - Включите webhook на URL: `https://ваш-адрес.up.railway.app/webhook`
   - В ссылке на оплату передавайте параметры:
     - `price=1500` — сумма
     - `client_id=123456789` — Telegram user_id

## 🚀 Как запустить

1. Подключите этот репозиторий к Railway.app
2. Укажите переменные окружения
3. Загрузите `sertif.png`
4. Готово!

Сертификаты нумеруются автоматически: 0001, 0002, ...
